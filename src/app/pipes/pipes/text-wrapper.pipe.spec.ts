import { TextWrapperPipe } from './text-wrapper.pipe';

describe('TextWrapperPipe', () => {
  it('create an instance', () => {
    const pipe = new TextWrapperPipe();
    expect(pipe).toBeTruthy();
  });
});
